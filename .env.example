# PostgreSQL connection (used by drizzle-kit CLI: db:push, db:generate, db:studio)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=hydra

# Used by docker-compose.yml to initialize the PostgreSQL container
POSTGRES_PASSWORD=postgres
